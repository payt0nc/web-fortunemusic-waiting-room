name = "web-fortunemusic-waiting-room"
compatibility_date = "2024-12-25"
pages_build_output_dir = "./build/client"

# Cloudflare Pages configuration
# The build command will be run by Cloudflare Pages
# For manual deployment, use: wrangler pages deploy ./build/client --project-name=web-fortunemusic-waiting-room
